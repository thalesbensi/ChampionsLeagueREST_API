/*
import {Request, Response} from 'express';
import * as ClubService from'"../services/clubs-service"

export const getAllClubs = (req: Request, res: Response) => {
    const response = await ClubService.getClubs();
}*/
